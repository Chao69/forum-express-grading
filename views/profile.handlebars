<div class="container">
  <div class="row">
    <div class="col">
      <img src="{{ user.image }}" class="rounded-circle" alt="" style="max-width: 500px;">
    </div>
    <div class="col">
      <h6>{{ user.name }}</h6>
      <p>{{ user.email }}</p>
      <form action="/users/{{ user.id }}/edit">
      <button class="btn btn-primary">edit</button>
      </form>
    </div>
  </div>
</div>